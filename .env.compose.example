# ==============================================================================
# ENT EST Salé — Docker Compose Environment
# ==============================================================================
# LOCALHOST (development): just run   docker compose up -d --build
# SERVER deployment: change SERVER_IP below, then run the same command
# ==============================================================================

# ── SERVER DEPLOYMENT ─────────────────────────────────────────────────────────
# Set this to your server's public IP or hostname.
# Leave as "localhost" for local development.
SERVER_IP=localhost

# ── Project ───────────────────────────────────────────────────────────────────
COMPOSE_PROJECT_NAME=ent-est-platform
TZ=Africa/Casablanca
FRONTEND_PORT=80

# ── Keycloak + PostgreSQL ─────────────────────────────────────────────────────
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=ChangeMe_123!
KEYCLOAK_DB_NAME=keycloak
KEYCLOAK_DB_USER=keycloak
KEYCLOAK_DB_PASSWORD=ChangeMe_123!
DEV_ADMIN_PASSWORD=Admin_123!
DEV_TEACHER_PASSWORD=Teacher_123!
DEV_STUDENT_PASSWORD=Student_123!

# ── RabbitMQ ──────────────────────────────────────────────────────────────────
RABBITMQ_USER=ent
RABBITMQ_PASSWORD=ChangeMe_123!
RABBITMQ_VHOST=/

# ── MinIO ─────────────────────────────────────────────────────────────────────
MINIO_ROOT_USER=minio
MINIO_ROOT_PASSWORD=ChangeMe_123!
MINIO_DEFAULT_BUCKETS=ent-courses,ent-uploads,ent-logs

# ── Cassandra ─────────────────────────────────────────────────────────────────
CASSANDRA_PASSWORD=ChangeMe_123!
CASSANDRA_CLUSTER_NAME=ent-est-cluster
CASSANDRA_KEYSPACE=ent_est

# ── Redis ─────────────────────────────────────────────────────────────────────
REDIS_PASSWORD=ChangeMe_123!

# ── Mailpit (SMTP sandbox) ────────────────────────────────────────────────────
MAILPIT_SMTP_PORT=1025
MAILPIT_UI_PORT=8025

# ── MS-Auth-Core ──────────────────────────────────────────────────────────────
MS_AUTH_CORE_PORT=8010
AUTH_REALM=ent-est
AUTH_JWKS_URL=http://keycloak:8080/realms/ent-est/protocol/openid-connect/certs
AUTH_AUDIENCE=ent-gateway,ent-frontend
AUTH_VERIFY_AUDIENCE=true

# ── MS-API-Gateway ────────────────────────────────────────────────────────────
MS_API_GATEWAY_PORT=8008
GATEWAY_TIMEOUT_SECONDS=8
GATEWAY_CORS_ORIGINS=*

# ── MS-Identity-Admin ─────────────────────────────────────────────────────────
MS_IDENTITY_ADMIN_PORT=8013
IDENTITY_ADMIN_ALLOWED_ROLES=admin
KEYCLOAK_BASE_URL=http://keycloak:8080
KEYCLOAK_REALM=ent-est
KEYCLOAK_ADMIN_REALM=master
CASSANDRA_CONTACT_POINTS=cassandra
CASSANDRA_PORT=9042
CASSANDRA_USERNAME=cassandra
IDENTITY_ADMIN_PROFILE_TABLE=user_profiles

# ── MS-Course-Content ─────────────────────────────────────────────────────────
MS_COURSE_CONTENT_PORT=8011
MINIO_ENDPOINT=http://minio:9000
MINIO_COURSE_BUCKET=ent-courses
INTERNAL_API_TOKEN=dev-internal-token

# ── MS-Course-Access ─────────────────────────────────────────────────────────
# IMPORTANT: set this to http://YOUR_SERVER_IP:9002 on a real server
# so that file download links work from students' browsers.
MS_COURSE_ACCESS_PORT=8012
MINIO_SIGNING_ENDPOINT=http://localhost:9002
COURSE_DOWNLOAD_TTL_SECONDS=300

# ── MS-Exam-Assignment ────────────────────────────────────────────────────────
# Same as above — must match MINIO_SIGNING_ENDPOINT
MS_EXAM_PORT=8017
MINIO_SUBMISSIONS_BUCKET=ent-uploads
SUBMISSION_DOWNLOAD_TTL_SECONDS=300
EXAM_MAX_FILE_BYTES=52428800

# ── MS-Notification ───────────────────────────────────────────────────────────
MS_NOTIFICATION_PORT=8014
NOTIFICATION_RABBITMQ_URL=amqp://ent:ChangeMe_123!@rabbitmq:5672/
EVENTS_EXCHANGE=ent.events.topic
NOTIF_QUEUE=q.notification.user
NOTIFICATION_ENABLE_CONSUMER=true

# ── MS-Calendar-Schedule ──────────────────────────────────────────────────────
MS_CALENDAR_PORT=8015
CALENDAR_WRITE_ROLES=teacher,admin
CALENDAR_EVENTS_TABLE=calendar_events

# ── MS-Forum-Chat ─────────────────────────────────────────────────────────────
MS_FORUM_PORT=8016
FORUM_THREADS_TABLE=forum_threads
FORUM_MESSAGES_TABLE=forum_messages
FORUM_COUNTERS_TABLE=forum_thread_counters

# ── Prometheus + Grafana ──────────────────────────────────────────────────────
GRAFANA_PORT=3001
GRAFANA_ADMIN_PASSWORD=ChangeMe_123!
